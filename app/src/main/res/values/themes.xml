<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 全般 (Activity用) -->
    <style name="Theme.ToraOnsei" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/chip_selected</item>
        <item name="android:statusBarColor">@color/keyboard_bg</item>
    </style>

    <!-- IME専用テーマ -->
    <style name="Theme.ToraOnsei.Ime" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="android:windowBackground">@color/keyboard_bg</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowShowWallpaper">false</item>
    </style>
</resources>
